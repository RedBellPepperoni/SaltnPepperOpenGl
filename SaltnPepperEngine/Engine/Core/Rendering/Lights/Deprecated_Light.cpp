#include "Deprecated_Light.h"

namespace SaltnPepperEngine
{
    namespace Rendering 
    {
        Matrix4 Light::ComputeCascadeMatrix(size_t cascadeIndex, float aspectRatio, float fov, const Matrix4& viewMatrix) const
        {
            return Matrix4();
        }
    }
}
